define([
    'intern!tdd',
    'intern/chai!assert',
    'lodash',
    '../src/eventbus'
], function(tdd, assert, _, EventBus) {
    tdd.suite('EventBus', function() {

        tdd.test('simple functions', function() {

        });
    });
});
